function new_ints = intFix(ints)

new_ints = ints;
n = size(ints,1);

for i=1:n
    if new_ints(i,2) > pi
        new_ints(i,:) = [-pi, new_ints(i,2)-2*pi];
        
    end

    if new_ints(i,1) < -pi
        %stuff
    end
end
end